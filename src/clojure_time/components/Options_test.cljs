;(ns clojure-time.components.options-test
;  (:require [cljs.test :refer-macros [is deftest]]
;            [clojure-time.components.options :refer [options]]))
;
;(deftest options-test
;  (let [[elem] (options {:templates ["test"]} (atom {}))]
;    (is (= :div elem))))
